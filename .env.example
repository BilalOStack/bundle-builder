# Optional. Leave unset and the app renders from the bundled data/catalog.json,
# which is all a clean clone needs.
#
# Set this to use the bonus Express API instead (`npm run dev:all`).
# The app appends "/catalog", and silently falls back to the bundled copy if
# the API is unreachable.
VITE_API_URL=http://localhost:8787/api
